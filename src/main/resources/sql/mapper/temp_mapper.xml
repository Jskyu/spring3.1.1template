<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="temp_mapper">

	<resultMap type="examVO" id="examVO">
		<result column="id" property="id" />
		<result column="name" property="name" />
		<result column="age" property="age" />
	</resultMap>
	
	<select id="exam1" resultMap="examVO">
		select id, name 
		from
			person
	</select>
	
	<select id="exam2" parameterType="_int" resultMap="examVO">
		select id, name, age
		from
			person
		where
			id = #{id}
	</select>
</mapper>